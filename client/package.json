{
  "name": "client",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-scripts": "5.0.1",
    "socket.io-client": "^2.2.0"
  },
  "scripts": {
    "start": "SET PORT=4012 && react-scripts start",
    "build": "SET PORT=4012 && react-scripts build",
    "test": "SET PORT=4012 && react-scripts test --env=jsdom",
    "eject": "SET PORT=4012 && react-scripts eject"
  },
  "proxy": "http://localhost:4011"
}
